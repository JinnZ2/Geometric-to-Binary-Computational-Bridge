{
  "schema": 1,
  "license": "CC0",
  "note": "Failure shapes, compressed. Roughly 60 findings across this archive collapse to these; several appeared in files that share no code, which is what makes them principles rather than bugs. A principle needs at least TWO independent instances -- one instance is an anecdote, and entries with one are marked PROVISIONAL rather than dropped or promoted.",
  "why_this_file": "Tags are for transfer and screening, not for storage and not for re-execution. You cannot re-run a candidate from its principles. What you CAN do is ask of a new candidate: which known failure shapes does this match? `mechanised_by` says which are caught automatically today; the ones with null are the gap list, and that list is the point.",
  "principles": [
    {
      "id": "P-UNFALSIFIABLE",
      "name": "An assertion no input can make fail",
      "status": "ESTABLISHED",
      "statement": "A check whose outcome does not depend on the thing being checked. It passes forever and the passing carries no information. Distinct from a wrong check: a wrong check can be caught by disagreeing with reality, this one cannot disagree with anything.",
      "detector": "Feed the check a deliberately broken artifact. If it still passes, the check is not a check.",
      "mechanised_by": "playground broken() gate; repo_guard CHECKLIST 'would make it FAIL'",
      "instances": [
        {
          "where": "experiments/silicon_speculative/vacuum_bounds.py",
          "what": "'at least one mode survives' -- the construction guaranteed a survivor",
          "claim": "VAC-1"
        },
        {
          "where": "experiments/silicon_speculative/topological_pin.py",
          "what": "topological_pin's original run() asserted a topological invariant is invariant",
          "claim": "ATT-1"
        },
        {
          "where": "tests/test_aiss.py",
          "what": "26 of 46 assertions were assertIsInstance / assertIn; a 40% score change did not move them"
        },
        {
          "where": "tests/test_gies_core.py",
          "what": "an `or True` inside an assertion"
        },
        {
          "where": "tests/test_field_claim_loop.py",
          "what": "a `pass` test body left in the field-loop suite by its own author"
        }
      ],
      "cost_when_missed": "A suite can be 79% dead and still look green. tests/test_silicon_modules.py was."
    },
    {
      "id": "P-NULL-ARTIFACT",
      "name": "The result survives replacing its structure with noise",
      "status": "ESTABLISHED",
      "statement": "If randomising the part you claim is doing the work reproduces the result, the result is a property of the shape, not of the content.",
      "detector": "Replace the structure with noise of the same shape and re-run. Compare rates, not anecdotes.",
      "mechanised_by": "repo_guard.null_harness; playground null() gate",
      "instances": [
        {
          "where": "Negentropic/lens_collapse_test.py",
          "what": "seventeen-lens isomorphism: randomly-coefficiented lenses of the same functional form reproduce the reported correlation floor",
          "claim": "NEG-7"
        },
        {
          "where": "AISS/sovereignty_evaluator.py",
          "what": "77% of random reweightings reproduce the flat-weight high-merit rate",
          "claim": "AISS-1"
        },
        {
          "where": "experiments/silicon_speculative/vacuum_geff_sim.py",
          "what": "three vacuum assertions that random matrices pass",
          "claim": "VAC-3"
        }
      ],
      "cost_when_missed": "A framework's central claim can be a property of its arithmetic."
    },
    {
      "id": "P-SELF-SUPPLIED-FALSIFIER",
      "name": "The model supplies the quantity that would falsify it",
      "status": "ESTABLISHED",
      "statement": "The test's reference value is produced by the same model under test, so agreement is guaranteed and measures nothing. The circularity is usually one indirection deep, which is why it survives review.",
      "detector": "Trace the target back. If it was computed rather than measured, and computed by the thing under test, the comparison is empty.",
      "mechanised_by": null,
      "gap_note": "NOT mechanised. The strongest unaddressed shape in this list, and the one that caught its own auditor -- see the lomb_scargle_gls instance.",
      "instances": [
        {
          "where": "Silicon/transient_suppression.py",
          "what": "the CMRR numerator was the theoretical value, not a measured one"
        },
        {
          "where": "Silicon/FRET/fret_core.py",
          "what": "the estimator WAS the generative process"
        },
        {
          "where": "Silicon/seed_influence.py",
          "what": "run_fixed_W's target was the model's own output"
        },
        {
          "where": "Silicon/keating_cluster.py",
          "what": "k_well taken from the DFT the cluster model was meant to test"
        },
        {
          "where": "playground/candidates/lomb_scargle_gls.py",
          "what": "TOL_FRAC = 0.01 is not derived -- it is what the estimator happened to achieve, so the tolerance check is partly circular. Recorded in that candidate's own archive entry."
        }
      ],
      "cost_when_missed": "A test that cannot fail, wearing the shape of one that can."
    },
    {
      "id": "P-SYMMETRY-COLLAPSE",
      "name": "The representation cannot see the operation that separates the states",
      "status": "ESTABLISHED",
      "statement": "An encoding is invariant under exactly the transformation that is supposed to distinguish its states, so half the state space is degenerate and nothing in the pipeline notices.",
      "detector": "Apply the distinguishing operation to a state and compare every invariant the representation exposes. If none move, the representation is blind to it.",
      "mechanised_by": null,
      "gap_note": "NOT mechanised. Two independent formalisms hit it and neither caught the other.",
      "instances": [
        {
          "where": "GEIS/gies_core.py",
          "what": "T = outer(v,v) cannot see the sign of v, so states i and 7-i are identical -- and NOT(i) = 7-i is precisely that map",
          "claim": "GIES-1"
        },
        {
          "where": "Silicon/keating_cluster.py",
          "what": "the clamped Keating energy is EXACTLY even in the central displacement, E(p) = E(-p), because two cross-terms vanish identically",
          "claim": "KEA-7"
        }
      ],
      "cost_when_missed": "An 8-state encoding that is really 4 states, with the gate set's only unary operation invisible."
    },
    {
      "id": "P-FIXED-PROBE",
      "name": "A detector that samples at one fixed point is blind wherever that point is a null",
      "status": "ESTABLISHED",
      "statement": "Probing a continuous space at a single hard-coded coordinate. Wherever the response happens to vanish at that coordinate, the detector has zero power, and the blind set is usually structured rather than rare.",
      "detector": "Write the response as a function of the probe coordinate and look for its zeros. Then measure power across the family, not at one member.",
      "mechanised_by": null,
      "instances": [
        {
          "where": "field/field_claim_loop.py",
          "what": "autocorrelation at fixed lag 3: rho(lag) = cos(2*pi*lag/T) is exactly zero at T=12, giving 0% power at periods 4, 10, 12, 14, 16 alongside a 22% false-alarm rate",
          "claim": "FCL-4"
        },
        {
          "where": "field/field_claim_loop.py",
          "what": "slot width derived from record span rather than sampling interval: each slot half a period wide, rho 0.23 at an arbitrary lag where the truth was -0.99",
          "claim": "FCL-12"
        },
        {
          "where": "Silicon/propulsion_bounds.py",
          "what": "traveling-wave phase aliasing at delta-phi = 2*pi*m/N -- the same blindness in the drive rather than the detector",
          "claim": "FP-2"
        }
      ],
      "cost_when_missed": "A detector with a 22% false-alarm rate and 0% power in the same configuration."
    },
    {
      "id": "P-UNCALIBRATED-GATE",
      "name": "A threshold asserted rather than measured against its own null",
      "status": "ESTABLISHED",
      "statement": "A cutoff picked because it looks reasonable, never run against data where the answer is known to be no. The error rate is then unknown in both directions, and 'too permissive' gets written in a comment instead of a number.",
      "detector": "Generate the null, run the gate, count. Then generate the effect and count again. Two numbers, both required.",
      "mechanised_by": "playground null() gate for candidates; nothing repo-wide",
      "instances": [
        {
          "where": "field/field_claim_loop.py",
          "what": "|rho| > 0.35 with no minimum-sample gate: 23.5% false alarm at n=5, 21.9% at n=8",
          "claim": "FCL-3"
        },
        {
          "where": "field/field_claim_loop.py",
          "what": "rate > 1.5x base with no significance test: 34.7%, 34.0%, 41.0% on null covariates -- and worse with more data",
          "claim": "FCL-5"
        },
        {
          "where": "Silicon/silicon_check.py",
          "what": "a thermal noise floor quoted without the bandwidth it was integrated over"
        }
      ],
      "cost_when_missed": "The folder's own notes said 'too permissive'. It was 41%."
    },
    {
      "id": "P-PREMATURE-SCALARIZATION",
      "name": "A shape reduced to a number at the door, and the reduction never revisited",
      "status": "ESTABLISHED",
      "statement": "Multi-dimensional evidence is projected to a scalar at ingest for convenience, the projection is not recorded, and every downstream claim is silently a claim about that one projection.",
      "detector": "Ask what was thrown away and whether anything downstream could have used it. If the answer is not recorded, it was thrown away silently.",
      "mechanised_by": "field reading() requires a shape to name its projection (partial -- naming it is not testing against it)",
      "instances": [
        {
          "where": "field/field_claim_loop.py",
          "what": "shape documented canonical, no code path reads it; test() compares value only",
          "claim": "FCL-9"
        },
        {
          "where": "GEIS/gies_core.py",
          "what": "the rank-1 tensor is a projection that discards the sign",
          "claim": "GIES-1"
        },
        {
          "where": "Negentropic/consciousness_metric.py",
          "what": "M(S) collapses a variance and a power into one score"
        }
      ],
      "cost_when_missed": "Storing the shape without testing against it is scalarizing with a receipt."
    },
    {
      "id": "P-UNITS-UNSTATED",
      "name": "A threshold on a quantity with no units",
      "status": "ESTABLISHED",
      "statement": "A number compared against a bound where the two do not share dimensions, or where the quantity's dimensions were never worked out. The comparison is then not wrong, it is meaningless.",
      "detector": "Write the units of both sides. If either cannot be written, there is no threshold.",
      "mechanised_by": "repo_guard.reach for instrument floors; CHECKLIST 'UNITS AND ORDERS' for the rest",
      "instances": [
        {
          "where": "Negentropic/consciousness_metric.py",
          "what": "M(S) >= 10: D is a variance and L a power, so the ratio has units and 10 is not a threshold on anything"
        },
        {
          "where": "Silicon/er_bounds.py",
          "what": "energy per bit quoted as 1-2 aJ, 0.1 eV and 0.01 eV in one archive; the last is 0.56 kT ln2, below Landauer",
          "claim": "SI-E"
        },
        {
          "where": "field/field_claim_loop.py",
          "what": "'lag 3' meant three residuals ago, not a duration, on an irregular clock",
          "claim": "FCL-12"
        },
        {
          "where": "field/field_claim_loop.py",
          "what": "the conservation ledger's reservoir, still unstated -- which is why it was not guessed",
          "claim": "FCL-5e"
        }
      ],
      "cost_when_missed": "A threshold that cannot be met or missed."
    },
    {
      "id": "P-STALE-PATH",
      "name": "Code that stopped running, and nothing reported it",
      "status": "ESTABLISHED",
      "statement": "A test, falsifier or branch that no longer executes -- moved import, missing bootstrap, unreachable guard -- while the surrounding report still looks healthy.",
      "detector": "Count what ran, not what passed. An import error inside setUp is reported as a test ERROR, not a collection failure.",
      "mechanised_by": "the full-sweep command with a per-file failure echo; nothing automatic",
      "instances": [
        {
          "where": "tests/test_silicon_modules.py",
          "what": "test_silicon_modules.py: 52 of 66 tests errored in setUp after a directory reorg moved six modules one level down"
        },
        {
          "where": "tests/test_bridges.py",
          "what": "nine test files lacked the sys.path bootstrap, so the documented command raised ModuleNotFoundError"
        },
        {
          "where": "field/field_claim_loop.py",
          "what": "the NOVEL route was unreachable whenever an anchor was logged -- a branch that could not fire",
          "claim": "FCL-1"
        },
        {
          "where": "Negentropic/maintenance.py",
          "what": "two falsifiers implemented and never run against data",
          "claim": "NEG-2"
        }
      ],
      "cost_when_missed": "A suite 79% dead that still looks like it ran."
    },
    {
      "id": "P-DUPLICATE-AUTHORITY",
      "name": "The same content in two places with no stated primacy",
      "status": "ESTABLISHED",
      "statement": "Two artifacts carry the same claim and nothing says which is canonical, so they drift and a reader cannot tell which one a downstream file depended on.",
      "detector": "Hash the bodies. Then ask which one the code imports.",
      "mechanised_by": null,
      "instances": [
        {
          "where": "AISS/AISS1.md",
          "what": "AISS.md and AISS1.md: byte-identical bodies, 1804 lines, differing only in preamble"
        },
        {
          "where": "GEIS/GIES_AUDIT.md",
          "what": "GIES.md 7.2 specified the correct weighted sum; 8.3 implemented a degenerate special case of its own spec"
        },
        {
          "where": "Negentropic/lenses.py",
          "what": "the 17 lenses defined twice before being centralised"
        }
      ],
      "cost_when_missed": "The implementation quietly wins over the specification, and the specification is what gets read."
    },
    {
      "id": "P-CENSORED-INPUT",
      "name": "Filtering before analysis biases rather than blinds",
      "status": "PROVISIONAL",
      "provisional_because": "one strong instance. It needs a second, independent one before it earns ESTABLISHED. Kept because the instance was decisive and the shape is general.",
      "statement": "Discarding the samples that did not trigger, then analysing what remains. The intuition is that you lose sensitivity; what actually happens is that you keep it and shift the answer, which is worse because the result still looks confident.",
      "detector": "Run the analysis on the full record and on the filtered one. Compare the answers, not just the detection rates.",
      "mechanised_by": null,
      "instances": [
        {
          "where": "field/field_claim_loop.py",
          "what": "the correlation branch was fed only band-breaking readings: 120 became 87, and it still came back STRUCTURED -- at lag 4 against a true half-period of 5, |rho| 0.79 versus 0.95 uncensored",
          "claim": "FCL-11"
        }
      ],
      "cost_when_missed": "A wrong answer delivered confidently, which is worse than silence."
    }
  ],
  "instance_fields": {
    "where": "repo-relative path, checked to exist. Always present.",
    "claim": "a claim or problem id from claims_index.py, when one really exists. Absent when it does not -- 19 of 36 instances originally carried an invented shorthand id that resolved nowhere, so the id is now optional and the location is not.",
    "what": "one line of prose"
  }
}
