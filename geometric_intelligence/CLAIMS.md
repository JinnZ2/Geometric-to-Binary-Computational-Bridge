<!-- GENERATED by claims_index.py render -- do not edit.
     Statements and statuses come from CLAIMS_REGISTER.json.
     Evidence class and file lists are SCANNED, not stored.
     Regenerate: python claims_index.py render -->

# Claims owned by `geometric_intelligence`

## GB

| id | statement | status | names | can fail via | salvage |
|----|-----------|--------|-------|--------------|---------|
| GB-1 | verify_edge_measurement computes delta_pct = 100*(measured/pred - 1) and raises ZeroDivisionError whenever the claim's predicted value is zero | live | refutation | FALSIFIER | **CODE_ERROR** — Zero is reachable two ways that both occur in the shipped demo's own domain: a rotate edge declares an angle and 0 is legal, and an acyclic network's integrity claim is exactly 0.0. A relative error is undefined at zero for every claim type, not just this one -- the ledger's other domains compare against nonzero physical rates, which is why the pattern survived there. |
| GB-2 | an acyclic design records an integrity claim of value 0.0, so a correct chain scores worst and writes the value GB-1 crashes on | live | refutation | FALSIFIER | **CODE_ERROR** — Not a bug in audit(): a network with no cycles genuinely has nothing to be consistent about, and 0.0 is the honest answer to a question that should not have been asked. The bridge should decline to write an integrity claim for an acyclic network rather than write a zero, because the ledger cannot tell 'no evidence' from 'failed'. The demo's own DEMO 5 does exactly this. |
| GB-3 | the integrity claim's failure text describes a threshold test while the record encodes a two-sided band around the value | live | refutation | FALSIFIER | **CODE_ERROR** — A claim record carries both what a verifier will compute and what a human will read, and here they are different tests. The text also cites 3.618, which GI-12 shows that score cannot reach. Cheap general check: read the failure string against the tol_frac in the same record. |
| GB-4 | three unsourced tolerances (0.05 edge, phi^-9 cycle, 0.10 integrity) and a drift band of (1+t)^2 rather than a doubled tolerance | live | refutation | FALSIFIER | **UNITS** — The drift band is a real design choice and a defensible one -- it is multiplicative, which suits a ratio claim -- but it reads as 2x tolerance and diverges from it as t grows: at t = 0.5 it is [0.25, 2.25] rather than [0, 2]. Naming it in the docstring costs one line. The three tolerances need sources rather than defaults before any verdict from them means anything. |
| GB-5 | LEDGER is a relative path and append_geo_claims does an unlocked read-modify-write, matching the 23 sites in fabrication/ | live | refutation | FALSIFIER | **CODE_ERROR** — The relative path is the sharper half and it is not in the original analysis of fabrication/: the ledger written depends on the working directory, so running from the repo root and from fabrication/ produces two ledgers that do not know about each other. Fixing it in one of 24 places makes the split worse, so it stays until all 24 move together. Open problem GI-10. |

## GI

| id | statement | status | names | can fail via | salvage |
|----|-----------|--------|-------|--------------|---------|
| GI-1 | IntegrityMonitor predictions are seeded from the design value, not the measurement, so a measured error propagates nowhere | live | refutation | FALSIFIER | **CODE_ERROR** — The module answers 'is this part where the drawing says', which is a real question and the one it can be trusted for. What it does NOT answer, and reads as if it does, is 'given this part came out wrong, is the next one still compatible'. Naming which of the two a tool answers is most of the value of auditing it, and full_report now carries predictions_use_measurements so a caller cannot assume the second. |
| GI-10 | _no recorded statement_ | unregistered | - | PROSE | |
| GI-11 | cycle closure is evaluated at the single point x = 1, so any affine cycle map with a + b = 1 is reported consistent without being the identity | live | refutation | FALSIFIER | **MATH_ERROR** — The general rule is one line and it transfers: an affine map is two numbers, so testing it at one point cannot determine it. Either evaluate at two points or compare (a, b) to (1, 0) directly -- the code already computes both in _compose_transforms and then throws b away into a single scalar. Pure-scale cycles have b = 0 and are unaffected, which is exactly why every demo and every shipped test misses it: they build only scale edges, and `compose` is the kind that breaks it. |
| GI-11a | _no recorded statement_ | unregistered | - | PROSE | |
| GI-12 | integrity_score = base_score * (1 + mean_edge_weight), where weight is author-declared and only ever adds: zero confidence still scores the full consistency fraction | live | refutation | FALSIFIER | **UNITS** — Two quantities are being multiplied that answer different questions -- how much of the network closes, and how sure the author was -- and the product has the units of neither. Measured: weights alone move the score across 1.2298-1.7248 with the geometry untouched, and declaring weight 0.0 costs nothing because the term is (1 + w) rather than w. Reporting the two separately costs nothing and makes both readable. The same arithmetic explains why INTEGRITY_THRESHOLD is unreachable: the ceiling at w <= 1 is 2.0. |
| GI-12a | _no recorded statement_ | unregistered | - | PROSE | |
| GI-13 | correct() reaches cycle consistency by splitting the error across the cycle rather than restoring the wrong edge | live | refutation | FALSIFIER | **MATH_ERROR** — The distinction worth keeping: making a model self-consistent and making it correct are different operations, and only the first is decidable from the model alone. Measured on the drop's own injected defect -- an edge 23.6% high converges to +11.9% with its inverse partner at -9.5%, and audit() then reports 5/5 consistent. Any correction that only sees the residual has this property; restoring the right value needs an external reference, which is what the measurements are for. |
| GI-13a | _no recorded statement_ | unregistered | - | PROSE | |
| GI-14 | _inverse_apply returns float('inf') on a zero factor while its only caller guards with except (ZeroDivisionError, OverflowError) | live | refutation | FALSIFIER | **CODE_ERROR** — A sentinel return and an exception guard written by two authors for the same failure, neither aware of the other, so the guard never fires and inf enters a reconstruction as a value. Found for free by the interim core.py reconstruction, which RAISED here -- guessing the safer behaviour is how you miss a bug, and that is the reusable lesson about reconstructing a missing module. |
| GI-15 | hash() is documented as a hash of network topology and relations and is also a hash of insertion order | live | refutation | FALSIFIER | **CODE_ERROR** — to_dict() lists nodes and edges in insertion order, so the same network built two ways hashes differently -- which matters because the hash is what a ledger entry would use to say 'this is the artifact I predicted'. Sorting by node id in to_dict() fixes it. The shipped test hashes one object twice, so it asserts the property that cannot fail and not the one that is false. |
| GI-16 | three of the drop's 20 tests cannot fail: the audit bounds hold by construction, the hash test reuses one object, and the correct() fixture is already consistent | live | refutation | FALSIFIER | **UNFALSIFIABLE** — The fixture is the sharp one: 2.5 x 0.4 = 1.0 exactly, so a pair commented 'slightly wrong' is exactly right, and correct() -- the function with two of this folder's findings in it -- is never run on an inconsistent network by anything in the suite. Checking a fixture's arithmetic before trusting what a test covers is two lines and it is where the coverage actually went. The suite is kept and vendored as evidence rather than deleted. |
| GI-2 | the INTEGRITY_THRESHOLD clause in full_report is unsatisfiable: integrity is a product of two fractions so is at most 1.0, and the clause tests it against 1.809 | live | refutation | FALSIFIER | **MATH_ERROR** — phi^2 + 1 = 3.618 is a correct constant that does no work anywhere in the module, so removing it costs nothing and keeping it costs a reader's trust. The general form: a threshold and the quantity it gates must be shown to share a range before the comparison means anything -- one line, and it is the same check that killed BLOCKAGE in bridges/. |
| GI-3 | check() keeps the minimum-error prediction over all sources, so a design with two routes of different product is resolved in the measurement's favour and reported trusted | live | refutation | FALSIFIER | **CODE_ERROR** — The redundancy is free evidence and is being spent to hide a contradiction rather than to find one: two routes disagreeing IS the finding, and reporting the spread instead of the minimum turns the same computation into a design audit. On a path-consistent design the O(N^2) walk returns one value from every source, so nothing is lost by changing it. |
| GI-4 | reconstruct() implements neither the averaging nor the path weighting its docstring promises; the first edge to reach a node wins, so insertion order decides | live | refutation | FALSIFIER | **CODE_ERROR** — The docstring is a usable specification of what the function should do -- confidence-weighted average over trusted paths -- and it is worth keeping as the target rather than deleting to match the code. The measurable form of the defect is cheap: build the same graph twice with the edges added in each order and compare, which is a general test for any first-writer-wins propagation. |
| GI-5 | IntegrityReport.reconstructed is declared on the dataclass and never assigned, so it is None on every report the module produces | live | refutation | FALSIFIER | **CODE_ERROR** — A declared-and-never-written field is found by one loop over the constructor's outputs, and it is worth looking for because it usually means a second code path was planned and the first one shipped. Here full_report fills the same value from a separate dict, so the two are one field apart from being a duplicate authority. |
| GI-6 | reconstruct() returns {} from a single measurement, and a forward-only chain reports its first node untrusted on perfect measurements | live | refutation | FALSIFIER | **CODE_ERROR** — The rule is simple and belongs in the field guide rather than the code: a node nothing points at cannot be predicted, so build every network with edges in both directions, and reconstruction needs two surviving measurements that agree, not one. The drop's own timber-frame example had forward edges only and flagged a correctly cut brace; the README's nautilus example added both directions and did not. |
| GI-7 | the field guide's speed-of-sound correction was 6%; the true shift from +20 C to -40 C is 10.6% | live | refutation | FALSIFIER | **MATH_ERROR** — c(T) = 331.3 + 0.606*T is within 0.32% of the exact sqrt(1+T/273.15) form at -40 C and 0.52% at -50 C, so the linear approximation is fine over the stated range and only the arithmetic downstream of it was wrong. 343.0 m/s hardcoded across fabrication/ is the +15 C value. pipe_modes, box_modes, cylinder_modes and ka_check already accept c=, so this is a threading job from a recorded temperature, not a physics change. |
| GI-8 | core.audit()'s integrity_score responds to the geometry: randomising every edge factor drops it from 2.000 to a mean of 0.014 over 200 draws | live | mechanism | FALSIFIER | **MATH_ERROR** — The one positive result in this folder, and worth recording as one. The null harness is cheap here because a geometric network has an obvious structure-destroying perturbation -- randomise the edge factors, keep the graph and the weights. Running it first also bounds what the other findings can mean: audit() is measuring something real and measuring it wrongly, which is a different repair from measuring nothing. |
| GI-9 | _no recorded statement_ | unregistered | - | FALSIFIER | |

## GR

| id | statement | status | names | can fail via | salvage |
|----|-----------|--------|-------|--------------|---------|
| GR-1 | the phi band edges are not monotonic: the loop overshoots f_max and then appends it, so the last interval is reversed and empty | live | refutation | FALSIFIER | **CODE_ERROR** — Two defects for the price of one inspection: a band that no input can enter, and a band above it running past the f_max the caller asked for, so components between f_max and the overshoot are counted and everything above the overshoot is dropped silently. Any geometric-progression edge builder has this shape -- the exit test fires after the step, not before. |
| GR-2 | bands_per_octave is stored and never read; every value gives byte-identical band edges | live | refutation | FALSIFIER | **CODE_ERROR** — An argument that looks like the resolution control and is not. Found by the cheapest possible test -- construct with two extreme values and compare the derived state -- which is worth running against every constructor parameter that is supposed to change something. |
| GR-3 | the power-law fit is built from every populated band including the peaks it is then used to judge, contradicting its own comment | live | refutation | FALSIFIER | **MATH_ERROR** — The bias has a direction worth stating: including anomalies in the baseline raises it, so the method misses anomalies, and it misses more of them the more of the spectrum is anomalous -- weakest exactly when there is most to find. A robust fit or a leave-one-out baseline fixes it; the shape is P-SELF-SUPPLIED-FALSIFIER in a spectral estimator. |
| GR-4 | _no recorded statement_ | unregistered | - | FALSIFIER | |
| GR-5 | detect_injection flags 15.7% of bins when both series are independent draws of the same noise process | live | refutation | FALSIFIER | **NULL_ARTIFACT** — For iid exponential numerator and denominator the analytic false-alarm rate is 1/(1+t), which at t = 2.3156 is 30.2% -- so the measured 15.7% on Rayleigh amplitudes is the expected order and the gate is behaving exactly as its arithmetic says. The fix is the FCL-5/6 pair the field loop already needed: a permutation null and a correction over the number of bins tested. injection_score cannot be read as a rate until both exist. |
| GR-6 | detect_injection returns a dict with no injections key when called without a baseline, so the documented fallback raises KeyError in the caller | live | refutation | FALSIFIER | **CODE_ERROR** — The fallback itself is intended and documented; only the silent shape change is not. It fails for exactly the callers who captured no baseline -- the cold-workshop case the field guide is written for -- which is the general lesson: a fallback path is used by the least-equipped caller, so it needs the tightest contract, not the loosest. |
| GR-7 | _no recorded statement_ | unregistered | - | PROSE | |

24 of 31 recorded. `python claims_index.py status` checks that nothing is marked live which nothing can falsify.
