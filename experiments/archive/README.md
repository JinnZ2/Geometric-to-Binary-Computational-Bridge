# experiments/archive/

One-off scripts and research notebooks kept for reference but not part
of the active dispatcher / runner ecology. Pulled out of `experiments/`
to keep the top level focused on the live polyglot stack.

| File                          | What it is                                                                         |
|-------------------------------|------------------------------------------------------------------------------------|
| `sim.py`                      | Two unrelated simulations spliced together (bio-engineered node energy + octahedral lattice phase). Incomplete. |
| `run_99b.py`                  | One-shot 99-bit factorization harness with ScentTrail-resume. Test script, not library code. |
| `factorization_landscape.py`  | Research notebook: tests the "semi-permeable trough" hypothesis for `E = (a*b - N)^2`. Honest methodology, never integrated. |

If you want any of these back in active use, move the file up one
directory and reintegrate. Otherwise treat as read-only history.
