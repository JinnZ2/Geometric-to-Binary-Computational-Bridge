<!-- GENERATED by claims_index.py render -- do not edit.
     Statements and statuses come from CLAIMS_REGISTER.json.
     Evidence class and file lists are SCANNED, not stored.
     Regenerate: python claims_index.py render -->

# Claims owned by `Engine`

## ENG

| id | statement | status | names | can fail via | salvage |
|----|-----------|--------|-------|--------------|---------|
| ENG-1 | the reported geometric_speedup was a point-count ratio times a symmetry factor, with no clock in it; timed, the adaptive path is 2x-50x slower than the uniform baseline | live | refutation | FALSIFIER | **NULL_ARTIFACT** — The measurement is the reusable part and it took one afternoon: uniform grid vs adaptive octree, same sources, same optimizer, best-of-N wall clock. `generateUniformGrid` already existed and had never been called by the solver, so the baseline was there all along. What survives the correction is the geometry: at resolution 32 the octree evaluates 15x fewer points and the field evaluation over those points really is ~26x faster (ENG-5). The claim that was wrong is that this had been measured. |
| ENG-2 | naive_points was hardcoded to 32**3 while resolution was a documented parameter that reached nothing | live | refutation | FALSIFIER | **CODE_ERROR** — Fourth instance of the unread-parameter shape after GR-2's bands_per_octave, GLY-3's sub_glyphs and ASF-13's exploration_rate, and the detector is the same two-line sweep: run at both extremes and diff. The docstring named a 'uniform fallback' path that does not exist in the module, which is the tell -- a parameter documented for a code path nobody wrote. |
| ENG-3 | the reported figure multiplied in a symmetry reduction the solver explicitly does not take | live | refutation | FALSIFIER | **CODE_ERROR** — The call site's own comment says it: 'we compute all but report the potential reduction'. Measured, a symmetric configuration reports 1.50x more speedup while taking 1.89x more wall clock, so the metric moves opposite to the truth exactly where the geometry is best. The general form is worth keeping: a saving may enter an efficiency figure only if the code took it. |
| ENG-4 | averageSpeedup reported the last run while history accumulated every run and was read by nothing | live | refutation | FALSIFIER | **CODE_ERROR** — Small, measured rather than argued: two runs at 15.24 and 22.88 reported 22.9 against a true mean of 19.06. Kept because the name is the whole defect -- a field called an average that is not one is read as one, and the data to compute it properly was already being stored. |
| ENG-5 | SpatialGrid.createRegion emits one sample point per leaf region, so the solver makes one numpy call per point and defeats its own vectorisation | live | refutation | FALSIFIER | **CODE_ERROR** — This is the actionable half of ENG-1 and the reason the roadmap's near-term item is a measurement rather than a multiplier. Batching the same points into one call: 0.0023 s against 0.0597 s, a 26x overhead removed and a 27x gain on the field evaluation. But the octree decomposition costs 0.0645 s against the uniform path's 0.0713 s total, so batching alone lands at 1.07x end to end -- both halves have to come down, and knowing that before starting is worth more than the 26x. Left in place deliberately; the fix changes solver output ordering and the per-region efficiency averaging. |
| ENG-6 | simdEfficiency is a function of chunk length alone and reports 12.5% for every configuration ever run | live | refutation | FALSIFIER | **CODE_ERROR** — A metric that returns the same number for one charge and for four is a constant wearing the name of a measurement, and the cause is ENG-5: one point per region means one lane of eight, always. It becomes informative for free once ENG-5 is addressed, which is why it is reported with the constancy stated rather than removed. |

6 of 6 recorded. `python claims_index.py status` checks that nothing is marked live which nothing can falsify.
