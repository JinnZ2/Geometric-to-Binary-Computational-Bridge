<!-- GENERATED by claims_index.py render -- do not edit.
     Statements and statuses come from CLAIMS_REGISTER.json.
     Evidence class and file lists are SCANNED, not stored.
     Regenerate: python claims_index.py render -->

# Claims owned by `fabrication`

## TMP

| id | statement | status | names | can fail via | salvage |
|----|-----------|--------|-------|--------------|---------|
| TMP-1 | the mechanical resonance correction uses thermal expansion where the modulus term dominates: ~20x too small and opposite in sign | live | refutation | FALSIFIER | **PHYSICS_BOUND** — The general form is a comparison anyone can do before writing the correction: for steel alpha = 12e-6/C and dE/E ~ -2.4e-4/C, so a stiffness correction built from expansion alone is wrong by the ratio of the two coefficients. It survives a bench test because 0.024% against a +/-8% band is invisible -- which is why the check has to be done on the coefficients and not on the output. |
| TMP-1a | _no recorded statement_ | unregistered | - | PROSE | |
| TMP-2 | two reference temperatures inside one module -- 20 C for the material and component corrections, 15 C for the acoustic ones -- and normalize_measurement crosses them | live | refutation | FALSIFIER | **UNITS** — This is the field guide's contraction table again, in executable form: percentages against an unstated baseline, back-solving to more than one. In code it is measurable -- normalize_measurement passes ref 15 C into r_correct, whose TCR is the copper value AT 20 C, biasing every electrical normalisation by TCR*5 = 2.0%. One module-level REF_TEMP_C removes it. |
| TMP-2a | _no recorded statement_ | unregistered | - | PROSE | |
| TMP-3 | acoustic_f_correct uses the linear c(T) ratio and helmholtz_f_correct the exact sqrt(T_ref/T), for the same physics, with the docstrings swapped | live | refutation | FALSIFIER | **SUPERSEDED** — A Helmholtz resonance is an acoustic mode, so the two functions answer one question and disagree by 0.28% at -40 C. Keeping the exact form and deleting the linear one costs nothing, since sqrt is not the expensive part. The docstring inversion is the useful tell: the function calling itself 'simplified linear' is the exact one. |
| TMP-4 | mu_water states no validity range, is 53% low at 100 C, and returns the 0 C value for every temperature below zero | live | refutation | FALSIFIER | **PHYSICS_BOUND** — Two different problems in one function and both are worth naming. The exponential fit is anchored at 0 C and degrades monotonically -- +6% at 20 C, -11% at 50, -53% at 100 -- so it needs a stated range. And below 0 C water is ice: returning a liquid viscosity there is a category error, and a workshop at -40 C is exactly where a caller would hit it. A refusal is the honest return. |
| TMP-5 | c_air(T) = 331.3 + 0.606*T is within 0.52% of the exact sqrt form over the -50..+50 C range it claims | live | mechanism | FALSIFIER | **MATH_ERROR** — The correction the whole exercise needed, and it is sound: a prediction made at +20 C and read at -40 C is 10.6% off in frequency, against a +/-8% acoustic band. Worth carrying the error figure rather than the word 'valid', because 0.5% is fine against 8% and not against a 1% electrical band -- the same number is adequate and inadequate depending on the claim it feeds. |

5 of 7 recorded. `python claims_index.py status` checks that nothing is marked live which nothing can falsify.
